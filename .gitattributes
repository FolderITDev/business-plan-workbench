# Normalize line endings: store text as LF in the repo, check out native on each OS.
* text=auto eol=lf

# Treat common binary evidence formats as binary (no EOL conversion, no diff noise).
*.pdf binary
*.docx binary
*.xlsx binary
*.png binary
*.jpg binary

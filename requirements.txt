# Deterministic helper dependencies. No Anthropic SDK, no network runtime.
# Install:  pip install -r requirements.txt
#
# Versions are PINNED for reproducibility. These are known-stable releases that
# work on Python 3.10. If pip reports "no matching distribution" for any exact
# version (a newer line may have superseded it), see the note at the bottom.

# Local document conversion (PDF/DOCX/XLSX/HTML -> Markdown) for convert_documents.py.
# NOTE the [all] extra: recent MarkItDown ships format readers (PDF/DOCX/XLSX) as
# optional extras. Without [all], plain `markitdown` installs but fails on real PDFs.
markitdown[all]==0.1.2

# Richer CSV structural summaries in summarize_csv.py (falls back to stdlib csv if absent).
pandas==2.2.3

# Clean Markdown tables in CSV summaries (DataFrame.to_markdown).
tabulate==0.9.0

# ----------------------------------------------------------------------------
# Finalizing an exact, verified lock (recommended after your first install):
#   pip install -r requirements.txt
#   pip freeze > requirements.lock.txt
# That captures the EXACT versions pip actually resolved on your machine —
# including MarkItDown's sub-dependencies — so future installs are identical.
#
# If an exact pin above fails to resolve, run:  pip index versions markitdown
# tell me what it lists, and I'll bump the pin to the nearest stable line.
